<form method="post" action="{{ path('app_attribution_delete', {'id': attribution.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ attribution.id) }}">
    <button class="btn">Delete</button>
</form>
