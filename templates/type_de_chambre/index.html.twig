{% extends 'base.html.twig' %}

{% block title %}TypeDeChambre index{% endblock %}

{% block body %}
    <h1>TypeDeChambre index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for type_de_chambre in type_de_chambres %}
            <tr>
                <td>{{ type_de_chambre.id }}</td>
                <td>{{ type_de_chambre.libelle }}</td>
                <td>
                    <a href="{{ path('app_type_de_chambre_show', {'id': type_de_chambre.id}) }}">show</a>
                    <a href="{{ path('app_type_de_chambre_edit', {'id': type_de_chambre.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_type_de_chambre_new') }}">Create new</a>
{% endblock %}
